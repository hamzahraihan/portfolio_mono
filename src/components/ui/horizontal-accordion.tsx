'use client';

import React, { useState } from 'react';

function HorizontalAccordion() {
  const [activeTab, setActiveTab] = useState(0);
  return <div>HorizontalAccordion</div>;
}

export default HorizontalAccordion;
